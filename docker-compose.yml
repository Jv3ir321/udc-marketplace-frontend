services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: udc-marketplace-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      - VITE_API_URL=http://localhost:4000

